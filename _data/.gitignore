# This directory once held data generated by _scripts/, which is why everything
# here is ignored by default. Hand-written data files must be listed below or
# they will never be committed, and the site will build without them.
*
# Except this file
!.gitignore
# Hand-written source - required by /approaches/ and by each paper's page
!approaches.yml
